import React from 'react';
import { Typography, Box, Grid } from '@mui/material';

const Sponsors = () => {
  return (
    <Box sx={{ padding: '3rem 0' }}>
      <Typography variant="h3" align="center" sx={{ marginBottom: '1rem' }}>
        Sponsors
      </Typography>
      {/* Add content here */}
    </Box>
  );
};

export default Sponsors;
